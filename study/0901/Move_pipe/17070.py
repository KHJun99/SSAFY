# 백준_17070_파이프 옮기기 1 (G5)
"""
1. 문제 상황

- 크기가 N × N인 집이 주어진다. (격자 형태, 1 ≤ N ≤ 16)
- 집의 각 칸은 빈 칸(0) 또는 벽(1) 으로 구성된다.
- 처음에 파이프는 (1,1)과 (1,2) 칸을 차지하면서 가로 방향으로 놓여 있다.
- 목표는 파이프를 집의 **오른쪽 아래 칸 (N,N)**까지 옮기는 방법의 수를 구하는 것.

2. 파이프 이동 규칙

파이프는 3가지 상태를 가진다:

1. 가로 (─)
2. 세로 (│)
3. 대각선 (＼)

각 상태에서 가능한 이동은 다음과 같다:

현재 상태	가능한 이동	조건
가로	    → (가로)	이동하려는 칸이 벽이 아님
	    ↘ (대각선)	오른쪽, 아래, 오른쪽아래 3칸이 모두 비어 있어야 함

세로	    ↓ (세로)	이동하려는 칸이 벽이 아님
	    ↘ (대각선)	오른쪽, 아래, 오른쪽아래 3칸이 모두 비어 있어야 함

대각선	→ (가로)	오른쪽 칸이 비어 있어야 함
	    ↓ (세로)	아래 칸이 비어 있어야 함
	    ↘ (대각선)	오른쪽, 아래, 오른쪽아래 3칸이 모두 비어 있어야 함
"""
def side_check(x, y):


N = int(input())
house = [list(map(int, input().split()))for _ in range(N)]